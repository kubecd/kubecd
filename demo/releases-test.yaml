releases:
  - name: test-demo
    chart:
      dir: charts/demo-app
    trigger:
      image:
        repoPrefixValue: "image.registry"
        track: Newest
    values:
      - key: fullnameOverride
        value: "demo-app"
      - key: image.tag
        value: "3842bd0"
